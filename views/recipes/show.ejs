<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- <link rel="stylesheet" href="/stylesheets/style.css" /> -->
    <link rel="stylesheet" href="/stylesheets/show.css" />
    <title><%= recipe.name %></title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <div class="show-body">
        <h1><%= recipe.name %></h1>
        <div class="show-sidebar">
                <div class="show-actions">
                    <div class="show-image">
                        <% if (recipe.image) { %>
                            <img src="<%= recipe.image %>" alt="<%= recipe.name %>" style="max-width:300px; border-radius:8px;">
                         <% } %>
                    </div>
                    <div class="show-links">
                        <a href="/users/<%= user._id %>/recipes"> Back to Recipes</a>
                        <a href="/users/<%= user._id %>/recipes/<%= recipe._id %>/edit">Edit</a> 
                        <form action="/users/<%= user._id %>/recipes/<%= recipe._id %>?_method=DELETE" method="POST" style="display:inline;">
                        <button type="submit">Delete</button>
                        </form>       
                    </div>
                </div>
                <p><strong>Category:</strong> <%= recipe.category %></p>
                <p><strong>Meal Type:</strong> <%= recipe.mealType %></p>

        </div>
        <div class="show-recipe">
            <div class="show-ingredients-container">
                <h3>Ingredients</h3>
                <p><%= recipe.ingredients %></p>
            </div>
            <div class="show-instructions-container">
                <h3>Instructions</h3>
                <p><%= recipe.instructions %></p>
            </div>
        </div>
 
    </div>
</body>
</html>